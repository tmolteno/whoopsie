## FreeCAD files for Whoopsie

This file contains the FreeCAD file for the frame and the camera mount. There are two .STL files

* whoopsie-Frame.stl
* whoopsie-CameraMount.stl

That can be printed directly. Note that these are just generated using FreeCAD from the whoopsie.FCStd CAD model.
